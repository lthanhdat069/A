-- Jack Hub 💎 - Full Hub template (Fluent UI)
-- Giữ nguyên giao diện thật, chỉ thêm hướng dẫn ngay bên cạnh dòng cần sửa (như yêu cầu).
-- Sol created this for Jack. Chú ý: thay URL loadstring bằng nguồn bạn tin tưởng.

repeat task.wait() until game:IsLoaded()

-- 🟢 Nút mở menu chính
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Name = "JackHubGUI" -- 👈 đổi tên GUI nếu muốn (nhớ trùng trong code nếu đổi)

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.10615778, 0, 0.16217947, 0)
ImageButton.Size = UDim2.new(0, 40, 0, 40)
ImageButton.Draggable = true
ImageButton.Image = "http://www.roblox.com/asset/?id=103173399598412"  -- 👈 đổi ID ảnh nút mở menu tại đây (ảnh roblox)
ImageButton.Name = "OpenMenuButton" -- 👈 đổi tên nút nếu muốn

UICorner.CornerRadius = UDim.new(1, 10)
UICorner.Parent = ImageButton

-- Mở/đóng menu bằng phím End
ImageButton.MouseButton1Down:Connect(function()
    -- Gửi sự kiện giả để minimize/maximize theo Fluent thiết lập
	game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)

-- 🟣 Tải Fluent UI (UI library)
-- Nếu bạn muốn sử dụng thư viện Fluent khác, đổi URL bên dưới
local successFluent, Fluent = pcall(function()
    return loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
end)

if not successFluent then
    warn("Không thể tải Fluent từ URL mặc định. Nếu bạn có thư viện UI khác, thay URL trong mã. (Dòng: loadstring Fluent)")
    -- Bạn có thể dừng ở đây hoặc tiếp tục với UI cơ bản
end

repeat wait() until game:IsLoaded()

-- 🪟 Cửa sổ chính
local Window = Fluent:CreateWindow({
	Title = "Jack Hub 💎",         -- 👈 đổi thành tên script của bạn
	SubTitle = "Script vip xịn nhất từ Jack", -- 👈 đổi mô tả phụ
	TabWidth = 157,
	Size = UDim2.fromOffset(450, 300),
	Acrylic = true,
	Theme = "Aqua",                   -- 👈 đổi màu chủ đạo: Amethyst, Dark, Aqua, Light, Rose, Emerald, Crimson, Ocean
	MinimizeKey = Enum.KeyCode.End
})

-- 📑 Tạo 6 tab mặc định (theo cấu hình bạn xác nhận)
local Tabs = {
	Main    = Window:AddTab({ Title = "Main" }),   -- 👈 đổi tên tab nếu muốn
	Troll   = Window:AddTab({ Title = "Troll" }),
	Admin   = Window:AddTab({ Title = "Admin" }),
	Game    = Window:AddTab({ Title = "Game" }),
	Fun     = Window:AddTab({ Title = "Fun" }),
	Extra   = Window:AddTab({ Title = "Extra" })
}

------------------------------------------------
-- TAB Main: Các script cơ bản / tiện ích
------------------------------------------------
Tabs.Main:AddButton({
	Title = "Copy TikTok Jack",              -- 👈 tên nút
	Description = "Sao chép link TikTok của Jack",-- 👈 mô tả nút
	Callback = function()
		setclipboard("https://www.tiktok.com/@your_tiktok_here")  -- 👈 thay bằng link TikTok của bạn
	end
})

Tabs.Main:AddButton({
	Title = "Server Hop (placeholder)",
	Description = "Ví dụ: chức năng server hop (bạn phải thay implementation)", -- 👈 mô tả
	Callback = function()
		-- 👇 Đây là vùng placeholder: thay bằng function server hop cụ thể nếu bạn có
		warn("Server Hop được gọi — thay bằng implementation của bạn nếu cần.")
	end
})

Tabs.Main:AddButton({
	Title = "Open Discord",
	Description = "Sao chép link Discord của bạn",
	Callback = function()
		setclipboard("https://discord.gg/your_invite_here") -- 👈 thay link Discord
	end
})

------------------------------------------------
-- TAB Troll: các nút vui/troll (ví dụ, an toàn)
------------------------------------------------
Tabs.Troll:AddButton({
	Title = "Make Player Float (demo)",
	Description = "Demo effect troll (không hại) - chọn mục tiêu trong game để áp dụng",
	Callback = function()
		local plr = game.Players.LocalPlayer
		if plr and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
			local hrp = plr.Character.HumanoidRootPart
			local old = hrp.Anchored
			hrp.Anchored = true
			task.delay(2, function() hrp.Anchored = old end)
			-- 👈 chỉnh 2 -> số giây muốn neo người chơi
		else
			warn("Không tìm thấy character để demo effect.")
		end
	end
})

Tabs.Troll:AddButton({
	Title = "Fake Lag (local only)",
	Description = "Chỉ là hiệu ứng local để gây cười, không ảnh hưởng server.",
	Callback = function()
		for i = 1, 6 do
			game:GetService("RunService").Stepped:Wait()
		end
		warn("Fake lag (local) completed.")
	end
})

------------------------------------------------
-- TAB Admin: các script quyền (PLACEHOLDERS an toàn)
-- LƯU Ý: nếu bạn muốn load Infinite Yield hoặc CMD-X, đổi URL bên dưới tại chỗ chỉ dẫn.
------------------------------------------------
Tabs.Admin:AddButton({
	Title = "Infinite Yield (example)",
	Description = "Ví dụ: thay URL bằng raw script Infinite Yield nếu bạn muốn",
	Callback = function()
		-- Nếu bạn có URL tin cậy, bỏ comment dòng bên dưới và thay link:
		-- loadstring(game:HttpGet("https://raw.githubusercontent.com/username/repo/main/infiniteyield.lua", true))()
		-- 👈 THAY link ở trên bằng link raw tin cậy của bạn (nếu muốn)
		warn("Infinite Yield placeholder. Thay loadstring(...) bằng URL của bạn nếu cần.")
	end
})

Tabs.Admin:AddButton({
	Title = "CMD-X (example)",
	Description = "Ví dụ khác cho tool admin. Thay link nếu muốn.",
	Callback = function()
		-- loadstring(game:HttpGet("https://example.com/cmdx.lua"))() -- 👈 thay URL ở đây (nếu có)
		warn("CMD-X placeholder. Thay URL trong code nếu có.")
	end
})

------------------------------------------------
-- TAB Game: script theo game (mỗi nút là placeholder an toàn)
------------------------------------------------
Tabs.Game:AddButton({
	Title = "Blox Fruits Helper (placeholder)",
	Description = "Tự thêm script/farm tùy ý - hãy dùng source bạn tin tưởng",
	Callback = function()
		-- Ví dụ: nếu bạn có script auto-farm Blox Fruits, đặt loadstring ở đây
		-- loadstring(game:HttpGet("https://raw.githubusercontent.com/your/repo/main/bloxfruits.lua", true))()
		warn("Blox Fruits placeholder. Thay bằng loadstring URL của bạn.")
	end
})

Tabs.Game:AddButton({
	Title = "Da Hood Helper (placeholder)",
	Description = "Đặt script Da Hood nếu muốn",
	Callback = function()
		warn("Da Hood placeholder - thay bằng script thực tế nếu có.")
	end
})

------------------------------------------------
-- TAB Fun: bay, noclip, invis (ví dụ an toàn / placeholder)
------------------------------------------------
Tabs.Fun:AddButton({
	Title = "Fly (local demo)",
	Description = "Demo fly local (không dùng exploit mạng). Thay bằng script đầy đủ nếu muốn.",
	Callback = function()
		local plr = game.Players.LocalPlayer
		if not plr or not plr.Character then return end
		local humanoid = plr.Character:FindFirstChildOfClass("Humanoid")
		if humanoid then
			humanoid.PlatformStand = true
			task.delay(2, function() humanoid.PlatformStand = false end)
			-- 👈 2 giây demo: chỉnh thời gian nếu cần
		end
	end
})

Tabs.Fun:AddButton({
	Title = "Noclip (toggle local)",
	Description = "Noclip local-demo: toggle CanCollide. Chỉ demo local.",
	Callback = function()
		local plr = game.Players.LocalPlayer
		if not plr or not plr.Character then return end
		for _,v in pairs(plr.Character:GetDescendants()) do
			if v:IsA("BasePart") then
				v.CanCollide = not v.CanCollide
			end
		end
		warn("Toggled local noclip CanCollide state. (Demo)")
	end
})

Tabs.Fun:AddButton({
	Title = "Invisible (local)",
	Description = "Làm mờ local avatar (demo).",
	Callback = function()
		local plr = game.Players.LocalPlayer
		if not plr or not plr.Character then return end
		for _,v in pairs(plr.Character:GetDescendants()) do
			if v:IsA("BasePart") then
				v.Transparency = 1
			elseif v:IsA("Decal") then
				v.Transparency = 1
			end
		end
		task.delay(3, function()
			-- Khi demo xong, reset transparency về 0 (nếu muốn)
			-- Lưu ý: nếu avatar có cosmetics, có thể cần xử lý khác
			for _,v in pairs(plr.Character:GetDescendants()) do
				if v:IsA("BasePart") then
					v.Transparency = 0
				elseif v:IsA("Decal") then
					v.Transparency = 0
				end
			end
		end)
	end
})

------------------------------------------------
-- TAB Extra: liên kết, hướng dẫn, cập nhật
------------------------------------------------
Tabs.Extra:AddButton({
	Title = "Hướng dẫn chỉnh sửa script",
	Description = "Mở clipboard với hướng dẫn cơ bản",
	Callback = function()
		local guide = [[Hướng dẫn nhanh chỉnh sửa Jack Hub:
- Đổi tên menu: sửa Title trong CreateWindow (dòng Window:CreateWindow).
- Đổi ảnh nút mở menu: thay ID ở ImageButton.Image.
- Thêm script: tại các nút placeholder, thay loadstring(...) bằng URL raw script của bạn.
- Luôn kiểm tra nguồn script trước khi chạy.
]]
		setclipboard(guide)
		warn("Hướng dẫn đã được sao chép vào clipboard.")
	end
})

Tabs.Extra:AddButton({
	Title = "Sao chép ID nút",
	Description = "Sao chép ID asset của nút mở menu để bạn thay đổi nhanh",
	Callback = function()
		setclipboard("103173399598412") -- 👈 thay bằng ID mặc định hoặc của bạn
	end
})

-- Thêm một footer nhỏ trong GUI (ví dụ)
local Footer = Instance.new("TextLabel")
Footer.Parent = ScreenGui
Footer.BackgroundTransparency = 1
Footer.Position = UDim2.new(0.02, 0, 0.95, 0)
Footer.Size = UDim2.new(0.3, 0, 0.04, 0)
Footer.Text = "Jack Hub 💎 - Sol built"
Footer.TextColor3 = Color3.fromRGB(200, 200, 255)
Footer.TextScaled = true
Footer.Font = Enum.Font.Gotham

-- Kết thúc script: thông báo ready
print("Jack Hub 💎 loaded. Kiểm tra các chỗ có chú thích '-- 👈' để tùy chỉnh theo ý bạn.")